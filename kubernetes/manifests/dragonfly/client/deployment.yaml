---
apiVersion: apps/v1
kind: Deployment

metadata:
  namespace: dragonfly
  name: client

spec:
  replicas: 1
  selector:
    matchLabels:
      app: client
  template:
    metadata:
      labels:
        app: client
    spec:
      containers:
        - name: client
          image: ghcr.io/vipyrsec/dragonfly-client-rs:sha-214971f0fd5839aa75b78a25bf0c5cbc75859973
          imagePullPolicy: IfNotPresent
          envFrom:
            - secretRef:
                name: dragonfly-client-env
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            runAsNonRoot: true
            runAsUser: 10000
            runAsGroup: 10000
            readOnlyRootFilesystem: true
          volumeMounts:
            - mountPath: /tmp
              name: tmp-volume
      volumes:
        - name: tmp-volume
          emptyDir:
