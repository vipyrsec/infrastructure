---
apiVersion: v1
kind: Secret

metadata:
  name: dragonfly-loader-env
  namespace: dragonfly

data:
{{- $ctx := .Values.secrets.dragonfly.loader -}}
{{- range $key, $value := mustMerge $ctx.devEnv $ctx.env }}
  {{ $key }}: {{ $value | b64enc }}
{{- end }}
